import { AboutMe } from '@components/aboutMe';
import { Layout } from '@components/layout';

export const AboutMeTemplate = () => {
  return (
    <Layout image="/blendedimg2.png" title="Minha Jornada">
      <AboutMe />
    </Layout>
  );
};
